#include <stdio.h>
#include <stdlib.h>
#include <math.h>
int main()
{
    /*float T,H,s,v,u,a,t,c,b,p;
    printf("Enter the values of u,a,t,b,c,p : \n");
    scanf("%f,%f,%f,%f,%f,%f", &u,&a,&t,&b,&c,&p);
    v= u + (a * t);
    s= (u*t)+ (a*t*t)/2;
    T=(2*a) + sqrt(b+9*c);
    H=sqrt(b*b + p*p);

    printf("V = %f \n", v);
    printf("s = %f \n", s);
    printf("t = %f \n", T);
    printf("h = %f \n", H);*/
    int h;
    h = sqrt(124);
    printf("%d",h);
    return 0;
}
