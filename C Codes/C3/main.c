#include <stdio.h>
#include <conio.h>
void main()
{
    char a,b[8];
    //a= getch(); it will only print the the printed output
    //a=getche(); it will print immediately any key is pressed for input
    /*a=getchar(); it will print only after enter key is pressed*/
     putchar(a);
     printf("Enter a string:  ");
        a=getch();
    //scanf("%s", &b);
    printf("Input String is: %s", b);
    return 0;
    puts(b);
}

